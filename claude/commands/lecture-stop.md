Stop the current background recording, transcribe the audio, and generate notes.

Use the Bash tool to run:
```
{{LECTURE_NOTES_PATH}}/.venv/bin/python {{LECTURE_NOTES_PATH}}/notes.py record-stop
```

Show the full output. When done, tell the user where the notes file was saved and print the notes.
